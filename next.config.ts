import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'media.istockphot.com',
      't3.ftcdn.net',
      'necessary-ladybug-115.convex.cloud',
      'upload.wikipedia.org',
      'images.unsplash.com'
    ]
  }
};

export default nextConfig;
